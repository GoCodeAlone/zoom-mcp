{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "to_channel": {
      "type": "string",
      "description": "Channel ID to list messages from."
    },
    "to_contact": {
      "type": "string",
      "description": "Contact email or user ID to list direct messages with."
    }
  },
  "oneOf": [
    { "required": ["to_channel"] },
    { "required": ["to_contact"] }
  ]
}
