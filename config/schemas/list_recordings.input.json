{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "properties": {
    "from": {
      "type": "string",
      "format": "date",
      "description": "Start date for the query range (YYYY-MM-DD). Defaults to current date minus 1 month."
    },
    "to": {
      "type": "string",
      "format": "date",
      "description": "End date for the query range (YYYY-MM-DD). Defaults to current date."
    }
  }
}
